# LoraLeya — устойчивые решения

- 2026-08-22 — Production и `main` не изменять без отдельного подтверждения: это действующий сайт и default branch.
- 2026-08-22 — Staging-first: все изменения сначала проверяются на закрытом `/test/`.
- 2026-08-22 — Ручные approval dialogs Codex сохраняются: критичные действия требуют явного контроля пользователя.
- 2026-08-22 — Историческое решение: индивидуальный заказ оплачивается по схеме 50% + 50%. Решение отменено и заменено правилами от 2026-08-23 ниже.
- 2026-08-22 — Историческое решение: доставка индивидуального заказа оплачивается отдельно по согласованному способу. Решение уточнено и заменено правилами доставки индивидуальных заказов от 2026-08-23 ниже.
- 2026-08-22 — YooKassa сама сообщает клиенту о факте оплаты и чеке; письма LoraLeya не дублируют это подтверждение.
- 2026-08-22 — `customer_processing_order` означает, что заказ принят LoraLeya в работу.
- 2026-08-22 — `customer_completed_order` означает, что заказ передан перевозчику и находится в пути.
- 2026-08-22 — Completed email содержит универсальный трек-номер заказа, если он заполнен.
- 2026-08-22 — Для ПВЗ сообщается, что служба доставки уведомит о готовности к получению.
- 2026-08-22 — Для курьера используется фраза «Курьер свяжется с вами перед доставкой.»
- 2026-08-22 — 5Post всегда рассматривается как доставка в ПВЗ; отправления создаются менеджером вручную.
- 2026-08-22 — Технический публичный товар для индивидуальных заказов не создавать; `product_id = 0` сохраняется.
- 2026-08-22 — Fivepost metabox guard для индивидуальных заказов сохраняется, чтобы защищать admin от `product_id = 0`.
- 2026-08-22 — `customer_note` обычного заказа остаётся максимально коротким: приветствие, номер, заметка, бренд-футер.
- 2026-08-22 — Клиентские письма исправляются изолированно, одно за другим; массовый рефакторинг email architecture без подтверждённой необходимости не выполнять.
- 2026-08-22 — После завершённого логического блока создавать checkpoint/commit и обновлять состояние проекта.
- 2026-08-22 — Критические персональные и секретные credentials никогда не помещать в документацию.

## 2026-08-23 — Индивидуальный заказ: 100% стоимости изделия

Решение:

- схема 50/50 отменена;
- после полного согласования параметров, срока и стоимости клиент оплачивает 100% стоимости изделия до начала изготовления;
- доставка в эту сумму не входит.

Почему:

- упрощение customer и WooCommerce workflow;
- не требуется второй платёж за изделие;
- стоимость доставки индивидуального заказа может зависеть от фактических габаритов и веса после изготовления.

Последствия:

- публичный FAQ и оферта должны соответствовать этому правилу;
- будущий WooCommerce workflow индивидуального заказа строится под один платёж за изделие.

## 2026-08-23 — Доставка индивидуальных заказов

Решение:

- 5Post: Москва и Московская область — бесплатно, другие регионы — 250 ₽; эти 250 ₽ не входят в первоначальную оплату стоимости изделия и оплачиваются покупателем отдельно до передачи заказа в 5Post; способ доступен только при соответствии фактических габаритов и веса требованиям доставки через постамат;
- если доставка через 5Post невозможна, выбирается другой доступный способ;
- СДЭК и Яндекс: покупатель оплачивает доставку при получении по тарифам выбранной службы доставки;
- LoraLeya бесплатно передаёт или доставляет готовый заказ до выбранной службы доставки.

## 2026-08-23 — Регламент использования Codex и экономии лимита

- Codex используется только для конкретных технических задач; одна логическая задача выполняется за один запуск.
- Повторные аудиты и тесты без новой причины не выполняются.
- Предпочтение отдаётся минимальным patch и переиспользованию уже проверенного кода.
- Визуальные и простые staging-проверки выполняются вручную, если Codex для них не нужен.
- Уровень интеллекта — Средний, Высокий или Очень высокий — выбирается по риску задачи.
- Задание даётся одним цельным копируемым блоком.
- Manual approvals остаются включёнными.
- Полный регламент хранится в `PROJECT_STATE.md`.

## 2026-08-24 — Individual-order workflow до оплаты

- Подтверждение individual order выполняется ответным email клиента, а не внешней web-страницей; web-confirmation и token больше не используются.
- Данные будущей доставки собираются в том же ответе клиента и структурированно переносятся из ИЗ-заявки в Woo order.
- Конкретный перевозчик выбирается после изготовления, когда известны фактические вес и габариты.
- Initial Woo order не содержит shipping item и оплачивается только в размере 100% стоимости изделий.
- Standard admin `WC_Email_New_Order` для individual orders отключён; обычные Woo orders не затронуты.
- Customer invoice должен явно показывать связь номера ИЗ-заявки с номером Woo order.

## 2026-08-24 — Позиционная модель individual order

- Individual order использует структурированную позиционную модель `items_v2` вместо модели формы стола и количества персон.
- Каждое сочетание изделия, размера и цвета является отдельной позицией; разные цвет или размер не объединяются.
- После согласования каждая позиция создаётся отдельным Woo line item со своими quantity, unit price и line total.
- Legacy-заявки и legacy-заказы продолжают обслуживаться старым путём.
- Custom Woo items сохраняют `product_id = 0`; технический или фиктивный WooCommerce-товар не создаётся.

## 2026-08-24 — Контакты и доставка в первоначальной заявке

- Клиент сразу сообщает ФИО, телефон, email и адрес доставки.
- Первоначальная форма не перегружается обязательным выбором города, ПВЗ, курьера или конкретного перевозчика.
- Способ получения и окончательные условия доставки менеджер уточняет при согласовании.
- Доставка не входит в первоначальную оплату стоимости изделий.

## 2026-08-24 — Письмо согласования

- Письмо после таблицы условий содержит только блоки «Как подтвердить заказ», «Что будет дальше» и «Остались вопросы?».
- Клиента не просят повторно сообщать уже полученные ФИО, телефон, адрес или способ доставки.
- Подтверждение выполняется ответным письмом с фразой `Подтверждаю заказ ИЗ-XXX`.

## 2026-08-24 — YooKassa и маркировка individual items

- Боевой YooKassa callback не перенастраивается ради `/test/`; автоматическую смену статуса проверять при production rollout / production-safe тесте.
- Vendor-файлы YooKassa не редактируются.
- Для productless individual items используется интеграционный слой LoraLeya, совместимый со штатным UI и `_marking_fields` YooKassa.
- Типы `tablecloth`, `runner`, `napkins`, `kuverts`, `curtains` получают параметры маркировки товаров лёгкой промышленности; `other` автоматически не классифицируется.
- `product_id = 0` остаётся неизменным.
- Финальный вывод КИЗ из оборота и фактический второй чек отложены до production-safe теста, поскольку по тестовому платежу №946 был запрошен возврат.
- Раннее отображение `+` до статуса `Обработка` сейчас считается не блокирующим нюансом и отдельно не исправляется.

## 2026-08-24 — Staging как источник истины для production

Это решение заменяет любые прежние предположения о том, что production нельзя полностью заменить staging-БД или что production потребуется вручную доводить параллельно второй раз.

Решение:

- staging `/test/` является источником истины и будущей финальной версией production;
- пока на старом production отсутствуют реальные заказы, финальная миграция выполняется как полный перенос протестированного staging-сайта, включая БД, в корень домена;
- сложный merge staging и production БД не выполняется;
- production не дорабатывается вручную параллельно; исключения — environment-specific проверки и настройки после миграции;
- до миграции `/test/` закрыт от индексации, причём защита не должна опираться только на `robots.txt`;
- существующие production URLs и slug сохраняются для SEO; новые версии страниц заменяют старые на тех же URL;
- URL replacement `/test/` → production выполняется serialized-safe инструментом, а не слепым SQL replace;
- перед открытием production обязательно проверяются отсутствие `noindex`, корректные robots, canonical, sitemap и внутренние ссылки;
- staging test data очищаются или архивируются до финального переноса после определения полного списка объектов и зависимостей;
- после миграции `/test/` закрывается, удаляется или архивируется и не остаётся публичной индексируемой копией;
- боевой YooKassa callback не меняется ради staging;
- финальный second receipt и фактический вывод КИЗ из оборота проверяются одним controlled production acceptance order.

Фазовая модель:

1. **Finish staging:** SEO protection, analytics, legal/business texts, email, privacy, quantity > 1 marking, mobile и normal-order regression, versions/settings, credentials и test-data cleanup.
2. **Freeze:** final regression, final checkpoint, полный production backup.
3. **One-time migration:** полный staging → production перенос, serialized-safe URL replacement, environment и SEO checks.
4. **Production acceptance:** небольшой реальный заказ, webhook, чеки, marking/КИЗ, second receipt, вывод из оборота и аналитика.

Partial refund маркированного заказа остаётся отдельной известной задачей и не блокирует rollout при условии, что непроверенный automatic partial refund не используется как подтверждённый workflow.

## 2026-08-25 — Финальная модель доставки individual order

- Основной Woo individual order является источником истины по актуальным данным и фактическим условиям доставки; ИЗ-заявка сохраняет первоначальные данные как историю.
- Оплата доставки имеет состояния `free`, `invoice`, `buyer` и техническое временное `undecided`.
- При `invoice` создаётся отдельный Woo order только с `WC_Order_Item_Shipping`; product lines и КИЗ в него не копируются.
- Поздняя доставка никогда не изменяет product total основного individual order.
- Настоящие Woo ID delivery orders сохраняются техническими. Публичная нумерация строится от основного заказа: `№YYY/Д`, `№YYY/Д-2`, `№YYY/Д-3`.
- Technical delivery payment orders с `_ll_delivery_payment_order=yes` скрываются из обычного клиентского списка My Account, но остаются доступны admin, customer invoice, прямому order-pay и YooKassa.
- Active unpaid delivery invoice блокирует delivery data и финансовые условия. После paid разрешена коррекция только ФИО и телефона; доплаты/refunds являются отдельным будущим workflow.
- Основной individual order нельзя штатно переводить в Completed при `undecided` или неоплаченном `invoice`; `free` и `buyer` завершение не блокируют.
- Customer-facing surfaces не показывают внутреннюю формулировку manager confirmation и method `ll_manager_confirmation`; internal/admin data не меняются.
- Individual-order development на staging считается завершённым. По этому workflow остаются только production acceptance реальной оплаты delivery invoice через боевой callback, second receipt / вывод КИЗ из оборота и marking quantity больше одного.

## 2026-08-26 — Источник публичного и юридического контента перед migration

- Актуальным источником истины является staging `/test/`; старый production-контент по оферте, оплате/доставке и возвратам считается устаревшим и не используется для обратной сверки.
- Будущий production формируется полным переносом текущего staging после freeze и обязательных migration-проверок.
- Staging-оферта, «Оплата и доставка» и «Возврат и обмен» должны соответствовать текущему runtime обычных и индивидуальных заказов; актуальная оферта уточнена в пунктах 5.3, 5.7, 5.9 и 5.10, возвраты согласованы с разделом 6.
- Актуальная Политика обработки персональных данных уже подана в Роскомнадзор и не изменяется без отдельной юридической необходимости.
- До migration `/test/` остаётся `noindex`; перед открытием production все staging SEO-ограничения обязательно снимаются.
- Боевой счётчик Яндекс.Метрики подключается ближе к production launch и не должен учитывать `/test/`.

## 2026-08-26 — Variation UI не меняет данные WooCommerce

- Custom dropdown размеров является только presentation layer над штатным WooCommerce select.
- Native select остаётся источником variation value и событий `change`; внутренние option values, variation IDs, цены и SKU не переписываются.
- Пользовательские подписи размеров строятся из видимого native option label, а не из внутреннего slug/value.
- SKU сохраняется в WooCommerce, admin и внутренних процессах; на frontend single-product он только визуально скрыт scoped CSS.
- Стандартный механизм WooCommerce reviews сохраняется: вкладка скрывается только у товара без опубликованных отзывов.

## 2026-08-26 — Mobile WooCommerce cart

- Фактическая корзина сайта — WooCommerce Block Cart; mobile-оформление scoped через `body.woocommerce-cart` и `max-width: 640px`.
- Полные размеры скатерти и дорожки в корзине являются только frontend presentation: internal variation values, variation IDs, cart/order meta не переписываются.
- Variation meta текущего Woo Blocks использует `span`; обновление подписи должно переживать React/Woo Blocks re-render через `MutationObserver`.
- Готовый набор сохраняет штатные подписи и не участвует в добавлении ширины.
- Завершённую mobile-корзину не пересматривать без нового воспроизводимого дефекта.

## 2026-08-26 — Mobile regression закрыт

- Mobile checkout использует scoped поля `20px` и ограничения безопасной ширины; desktop и checkout workflow не меняются.
- Consent checkbox сохраняют отдельные тексты, required-состояние и server-side validation; mobile patch отвечает только за spacing и alignment.
- Полные размеры скатерти и дорожки в checkout являются presentation layer, повторяемым после `updated_checkout`; internal variation values, variation IDs и cart/order meta не переписываются.
- Оферта (`page-id-772`), Privacy Policy (`page-id-774`), «Оплата и доставка» (`page-id-776`) и «Возврат и обмен» (`page-id-778`) используют единое scoped mobile-правило с полями `20px`.
- Юридический контент не менялся; Privacy Policy остаётся неизменной по содержанию.
- Весь mobile regression считается PASS / CLOSED и повторно открывается только при новом воспроизводимом дефекте.
