# LoraLeya — daily 2026-08-26

## Checkpoint scope

Подготовлен checkpoint `checkpoint: pre-production checkout and policy alignment` в ветке `fix/staging-audit-2026-08-13`. Production, `main`, vendor и individual-order workflow не изменялись.

Код checkpoint:

- `inc/checkout-workflow.php`: два отдельных обязательных checkout consent, штатные dynamic URL Политики и оферты, отдельная server-side validation; standard-only order-received с заголовком «Заказ принят», карточкой «Что дальше» и заголовком «Контактные данные»; individual и delivery-payment orders исключены.
- `assets/css/checkout.css`: scoped desktop/mobile оформление `.ll-standard-order-received`, читаемый mobile `tfoot`, улучшенная читаемость текста «Что дальше».
- `functions.php`: standard order-received кнопка «← Вернуться в каталог» и dynamic `wc_get_page_permalink( 'shop' )`; `shop_url` передан в существующий JS-объект `loraleya`.
- `assets/js/main.js`: прежнее constructor/palette действие «+ Добавить в другом цвете» сохранено; на остальных страницах «+ Добавить товар» ведёт в основной WooCommerce shop; при пустой корзине кнопка скрыта.

## Ручные staging-проверки

### SEO `/test/`

- WordPress Search Engine Visibility включён.
- Meta robots: `noindex, nofollow`.
- HTTP `X-Robots-Tag`: `noindex, nofollow, noarchive, nosnippet`.
- SEO-защита `/test/`: PASS.
- Перед открытием production эти ограничения обязательно снять.

### Яндекс.Метрика

- Код Метрики в репозитории не найден.
- Запросов к `mc.yandex.ru` нет ни на `/test/`, ни на текущем production.
- Метрика сейчас не подключена.
- Боевой счётчик подключить ближе к production launch, исключив `/test/`.

### Юридические страницы

- Актуальный источник истины — staging; старый production игнорируется как устаревший.
- В оферте вручную уточнены пункты 5.3, 5.7, 5.9 и 5.10 для корректного различения оплаты доставки обычных и индивидуальных заказов.
- «Оплата и доставка» вручную приведена в соответствие оферте и runtime.
- «Возврат и обмен» вручную приведена в соответствие разделу 6 актуальной оферты.
- Политика обработки персональных данных не изменялась: она уже подана в Роскомнадзор и может меняться только при отдельной юридической необходимости.

### Privacy UI

- Individual-order form: отдельный незаполненный checkbox, ссылка `/test/privacy-policy/` — PASS.
- Обычный checkout: два отдельных обязательных checkbox; сценарии validation — PASS.

### Standard order-received

- Desktop и mobile — PASS.
- Товары, вариации, доставка и totals сохранены.
- «Контактные данные» отображаются корректно.
- Возврат в каталог работает.

### Cart widget

- Каталог: «+ Добавить товар» → `/test/shop/` — PASS.
- Palette/constructor: «+ Добавить в другом цвете», прежний workflow — PASS.

## Зафиксированные ограничения

- Production content по offer/delivery/returns устарел и не является источником истины.
- Будущий production — текущий staging `/test/` после полной migration.
- Staging SEO-защиту нельзя переносить на открытый production.
- Individual order остаётся CLOSED ON STAGING.
- Commit не отправляется в remote: push запрещён текущей задачей.

## Product cards и variation UI

Подготовлен checkpoint `checkpoint: refine product cards and variation UI`. Изменения ограничены карточками обычных WooCommerce-товаров; individual order, checkout/order workflow, `main` и production не изменялись.

Код:

- `functions.php`: скрыта вкладка Additional information / «Детали»; Reviews скрывается только при `0` опубликованных отзывов; штатный review mechanism сохранён; внутренний заголовок description panel убран, вкладка «ОПИСАНИЕ» остаётся.
- `assets/js/product-swatches.js`: добавлен доступный custom dropdown поверх native WooCommerce select. Native value и `change` остаются источником выбора вариации; внутренние values, variation IDs, цены и SKU не меняются. Размер берётся из видимого native label: скатерть `140 × 175/220/240 см`, дорожка `40 × 140/175/240/300 см`; набор сохраняет исходные названия. Keyboard, Escape и click outside сохранены.
- `style.css`: scoped responsive оформление dropdown с тёмным фоном, золотой рамкой и фирменным selected state без системного синего highlight; SKU скрыт только визуально на single-product, категория остаётся видимой.

Ручные staging-проверки:

- скатерть: custom dropdown, полные размеры, выбор вариации, добавление в корзину — PASS;
- дорожка: полные размеры и выбор варианта — PASS;
- готовый набор: исходные названия вариантов сохранены, dropdown — PASS;
- SKU скрыт; категория остаётся; «Детали», «Отзывы (0)» и второй заголовок «Описание» скрыты — PASS.

В WordPress БД `/test/` вручную обновлены описания скатерти, дорожки, салфетки, куверта и готового набора; в Git этих изменений нет. Зафиксированные правила: отдельный текст для каждого типа и реальные полные размеры; дорожки и салфетки обработаны «английским уголком»; салфетка `40 × 40 см` с допуском до `0,5 см`; куверт `9 × 24 см`, два отделения; скатерть шириной `140 см` и длиной `175/220/240 см`; дорожка шириной `40 см` и длиной `140/175/240/300 см`; набор содержит салфетки и куверты по числу персон плюс одну дорожку выбранной длины; присутствует предупреждение о переливе жаккарда и возможном отличии цвета из-за цветопередачи экрана.
