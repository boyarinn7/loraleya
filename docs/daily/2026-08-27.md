# LoraLeya — daily 2026-08-27

## Migration staging → production — PASS / CLOSED

Проверенное staging-состояние `/test/` успешно перенесено в production root:

1. **Runtime/filesystem — PASS.** Файловая часть migration выполнена; актуальная тема, runtime-код и рабочие media находятся на production.
2. **Database — PASS.** Production prefix `wptk_*` теперь содержит актуальную копию staging DB. Исходный staging prefix `wpstg0_*` сохранён отдельно.
3. **Cutover — PASS.** Production открыт; визуальный smoke production завершён успешно.

Старое production-состояние больше не используется как источник актуального контента.

## Post-migration visual fixes — PASS / CLOSED

После migration исправлены и вручную проверены на staging и production три визуальных дефекта:

- single product: заголовок больше не заходит под фиксированную шапку;
- individual order: поле «Изделие» использует фирменный custom dropdown без системной синей плашки Chrome/Windows;
- WooCommerce cart: checkout CTA приведена к основному стилю LoraLeya.

Runtime-файлы checkpoint: `style.css` и `assets/js/custom-order.js`. Дополнительные workflow, данные и плагины не менялись.

## Product SEO — PASS / CLOSED

Все пять опубликованных товаров повторно проверены. Длинные описания SEO-пригодны, уникальны, без серьёзных дублей и переспама и соответствуют размерам и вариациям.

- Product ID `44`, «Скатерть»: SEO title — `Жаккардовая скатерть для сервировки стола — LoraLeya`; meta description — `Жаккардовая скатерть LoraLeya для сервировки стола. Три размера на выбор, аккуратная обработка и выразительная фактура ткани.`
- Product ID `50`, «Готовый набор — всё в одном цвете»: SEO title — `Готовый набор столового текстиля для сервировки — LoraLeya`; meta description не менялся.
- Staging остаётся отдельной защищённой средой. Production открыт; technical SEO не перепроверялся в рамках текущего визуального checkpoint и учитывается отдельно.

## Uploads/media — PASS / CLOSED

Устаревший отсутствующий файл `/wp-content/uploads/2026/05/serebro-video.mp4` не восстанавливается.

- Attachment ID `793` сохранён в Media Library.
- Его `post_title` на staging изменён с `serebro-video` на `Архивное видео — не используется`.
- `/color/serebro/` больше не запрашивает отсутствующий MP4 и использует штатный Ken Burns fallback: `serebro-hero-servirovka`, `serebro-macro-pereliv`, `serebro-hero-detail`.
- Другие 16 цветовых страниц и theme code не изменялись.
- Повторная runtime-сверка: broken media = `0`.

Для страницы «О бренде» актуален `/wp-content/uploads/2026/08/natalia-kurenkova-loraleya.webp`. Старый одноимённый файл в `/2026/06/` существует, но текущий runtime его не использует.

## Backup и freeze — pre-cutover решение

- Staging заранее не замораживать.
- Не ждать ещё сутки ради следующего автоматического backup REG.RU.
- REG.RU уже создаёт отдельные backup файлов и БД; автоматическая резервная копия существует.
- Непосредственно перед migration требовалось создать или зафиксировать ещё одну свежую резервную копию production и staging и проверить возможность rollback.
- Этот раздел сохраняется как историческое pre-cutover решение; migration позднее в тот же день была завершена отдельной задачей.

## 5Post и старые тестовые операции

- 5Post на staging не менять; новый ключ установить уже после migration на production и затем минимально проверить карту и выбор ПВЗ.
- Не выполнять дополнительный reconciliation старых тестовых Woo/YooKassa заказов `867/874/875/946` только ради migration.
- Реальных клиентских заказов ещё не было.
- Очистка данных, если понадобится, остаётся отдельной задачей после свежего backup.

## Следующий безопасный шаг

Не менять production и staging без следующей отдельной задачи. После ответа менеджера установить новый 5Post key на production и минимально проверить карту/ПВЗ. Controlled production acceptance для callback, чеков и КИЗ и оставшиеся technical SEO/environment follow-up выполнить отдельно.

## Ограничения текущего checkpoint

- В рамках этого Git-checkpoint сайт, staging DB и production не изменяются; migration уже завершена ранее.
- Backup, cleanup и обновления плагинов не запускаются.
- Secrets, credentials и персональные данные в документацию не записываются.
- `LoraLeya_handoff_2026-08-26.md` остаётся untracked и в commit не включается.
