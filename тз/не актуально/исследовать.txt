элемент {
  background-image: url(https://loraleya.ru/wp-content/uploads/2026/04/biruza-macro-faktura-150x150.webp);
  background-size: cover;
  background-position: center;
}
.colors-wheel .color-swatch {
  width: 4.5rem;
  height: 4.5rem;
  border-radius: 50%;
  cursor: pointer;
  border: 1px solid rgba(197, 165, 90, 0.3);
  transition: all 0.3s ease;
  position: relative;
  flex-shrink: 0;
}
.color-swatch {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  cursor: pointer;
  border: 2px solid transparent;
  transition: all var(--transition);
}
a {
  color: inherit;
  text-decoration: none;
  transition: color var(--transition);
}
*, ::before, ::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
body {
  color: var(--text);
  font-family: var(--sans);
  font-weight: 300;
  line-height: 1.6;
}
:root {
  --bg: #0e0e0c;
  --text: #c8c0b4;
  --sans: 'Raleway', sans-serif;
  --transition: 0.3s ease;
}
:root {
}
:root {
  --wp--preset--font-size--normal: 16px;
  --wp--preset--font-size--huge: 42px;
}
:root {
}
html {
  font-size: 16px;
}
html {
  --wp-admin--admin-bar--height: 32px;