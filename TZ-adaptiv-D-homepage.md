# ТЗ Code-Клоду — Адаптив, компонента D: ГЛАВНАЯ

*Бридж от Спринт-Клода по `tz-adaptiv-mobile-tablet.md` (4.D). Компонента C закрыта. D — главная: один фикс (высота hero под iOS) + проверка.*

**Файл:** `style.css`
**Гейты:** rem; новый `!important` не плодить; ноль CLS; НЕ трогать разметку/тексты/логику.

**Что выяснено разведкой:**
- `.scenarios-grid` уже адаптивна: 3 кол → 2 кол на ≤1024 → 1 кол на ≤640. На 768 = 2 колонки, на 360 = 1. **Править не нужно — только проверить.**
- `.hero { min-height:100vh }` (и `85vh` на ≤640). На iOS Safari `100vh` заходит под адресную строку → низ (CTA «Выбрать сценарий») может обрезаться. **Это и чиним** — добавляем `svh` с фолбэком.

## 0. Git-чекпойнт
```bash
git add -A && git commit -m "checkpoint before adaptive-D homepage hero" --allow-empty
git tag pre-adaptive-D
```

## 1. Hero — базовая высота (добавить `svh` после `vh`)
**Найти:**
```css
.hero {
  position: relative;
  min-height: 100vh;
```
**Заменить на:**
```css
.hero {
  position: relative;
  min-height: 100vh;
  min-height: 100svh;
```

## 2. Hero — мобильная высота ≤640 (добавить `svh` после `vh`)
**Найти:**
```css
  .hero {
    min-height: 85vh;
    padding-top: 3rem;
```
**Заменить на:**
```css
  .hero {
    min-height: 85vh;
    min-height: 85svh;
    padding-top: 3rem;
```
> Почему `svh`, а не `dvh`: `svh` = высота при видимой адресной строке (самый маленький вьюпорт), поэтому контент гарантированно влезает и CTA виден без скролла. `dvh` меняется при скрытии/появлении строки и может дёргать вёрстку. Фолбэк `vh` остаётся первым — старые браузеры берут его, новые перекрывают на `svh`.

## 3. Что НЕ делаем
- `.scenarios-grid` не трогаем (уже корректная). `.products-strip`, `.colors-wheel`, `.brand-grid` — не трогаем, только смотрим в проверке.
- Разметку/тексты/логику — не трогаем.

## 4. Приёмка (1024 / 768 / 414 / 360; ключевое — реальный iPhone)
1. **iPhone, первый экран:** заголовок, подзаголовок и кнопка **«Выбрать сценарий»** видны **без скролла** — низ не обрезан адресной строкой. (Это цель фикса.)
2. **Сетка сценариев:** на 768 — **2 колонки** (6 карточек = 3 ряда по 2), на 360 — **1 колонка**, карточки не разъезжаются.
3. Нет горизонтального скролла на всех ширинах.
4. Кнопки/ссылки на главной жмутся пальцем.
5. Десктоп (>1024): hero и сетка как были (3 колонки, полный экран).

## 5. Откат
```bash
git reset --hard pre-adaptive-D
```

---
*Бэклог адаптива (не теряем): после D — **E (карточка товара)**: перенос 17 свотчей на 360, таб-бар Описание/Детали/Отзывы на узком экране, кнопка «В КОРЗИНУ» на мобиле. Затем **F (контрольный проход)**: цвет / About / блог на 768/360. Вне адаптива: стандартизация брейкпоинтов — отложено; гейтовые слова в `single-scenario.php` — контент-ТЗ; режим работы у Натальи. После всего — повторный PageSpeed mobile (респонсив бьёт по CLS/LCP), до снятия общего noindex.*
