# ТЗ Code-Клоду: вёрстка страницы «О бренде» по прототипу

**Источник:** `about-loraleya.html` (готовый прототип дизайнера). Воплотить этот дизайн на странице `/about/`.

## Способ интеграции — отдельный шаблон (решено 14.06.2026)
- Премиум-вид делается **отдельным шаблоном страницы** `/about/` (напр. `page-about.php`) с разметкой прототипа. CSS-стилями поверх редактора этот лейаут не получить.
- **Контент в редакторе страницы `/about/` после установки шаблона — ОЧИСТИТЬ.** Иначе текст задвоится (шаблон + `the_content()`). Шаблон рендерит свою разметку, редактор не используется.

## Главное правило (контент-гард)
**Источник текста — `block-about-loraleya-FINAL.md`, дословно.** Прототип даёт ДИЗАЙН и СТРУКТУРУ; все слова, ссылки, факты и манифест берём из FINAL. Если копия в прототипе где-то разойдётся с FINAL — приоритет у FINAL.

**Заголовки разделов — образные, как в прототипе** (H2), наши формулировки остаются «бровями» над ними:
- Блок 2: H2 «Текстиль, который собирает стол» / бровь «Что мы делаем»
- Блок 3: H2 «С 2022 года» / бровь «История»
- Блок 4: H2 «Мастерская в Подмосковье» / бровь «Как мы шьём»
- Блок 5: H2 «Пять причин остаться» / бровь «За что нас выбирают»
- Блок 6: H2 «Магия сервировки» (допустимо — строка из манифеста Натальи) / без брови
- Блок 7: H2 «Свяжитесь с нами» / бровь «Контакты и ателье»
- Блок 8: H2 «Готовы выбрать текстиль для своего стола?» / бровь «С чего начать»

Перед публикацией:
- Ctrl+F по «полиэстер», «синтетик» — должно быть пусто (стоп-лист).
- Наборы — «2–6 персон» (НЕ 2–12).
- «Монограммы» не добавлять (услугу не предлагаем).
- Цен в тексте не хардкодить (правило цен).

## Сохранить (не потерять при вёрстке)
- 3 блока Schema.org уже в `functions.php` через `is_page('about')` — НЕ удалять и НЕ дублировать.
- H1 = «О бренде LoraLeya» (с брендом). Слаг `/about/` не менять.
- Все внутренние ссылки из прототипа уже верные: `/color/bezhevyj/`, `/color/belyj/`, `/product/gotovyj-nabor/`, `/individualnyy-zakaz/`, `/#palette`, `/#scenarios`.
- Весь контент блоков 1–8 и манифест Натальи — дословно как в коде.

## Интеграция в WP
- Использовать шапку и подвал ТЕМЫ (единообразие с сайтом), не `topbar`/`footer` из прототипа. Из прототипа берём секции от hero до финального CTA + стили.
- CSS из `<style>` перенести в тему/страницу. Переменные совпадают с нашими (`--bg #0e0e0c`, `--gold #c5a55a`).
- Логотип/начертание — с главной (шапка темы это уже даёт).
- Круглую печать в hero взять с главной — блок `.brand-seal` из `front-page.php` + его CSS из `style.css` (единый логотип на главной и About), вместо seal-варианта из прототипа.

## Заметки дизайнера (выполнить)
1. **Шрифты.** Заменить превью-подключение Google CDN на боевые self-hosted Cormorant Garamond + Raleway. Веса не трогать: заголовки Cormorant 500, акцентные слова 500 italic; тело Raleway 300, «брови» и кнопки Raleway 500.
2. **Два фото** на месте плейсхолдеров (hero — Наталья в мастерской; Блок 4 — ателье), оба слота 3:4. Обработка под тёмную палитру: приглушить насыщенность, тёплый сдвиг, без выбитых белых — фото не должно выпрыгивать ярче фона.
3. **Золото — только акцент.** На фото без конкурирующих цветов (синий/розовый реквизит). Бронза/беж в кадре — идеально, ложится в палитру.
4. **Печать (seal) и цифры** (17 / 200+ / 4 / 7–14) — проверить на retina: тонкие золотые штрихи рамки и цифр не должны мылиться.
Больше по визуалу ничего не трогать — hover, фокус и анимация на скролле уже в коде.

## Проверить
- В подвале прототипа ссылка `/catalog/` — такой страницы на сайте нет (есть `/shop/`). Если берём подвал темы — вопрос снят; если нет — поправить.

После вёрстки прислать скрин — SEO-Клод сверит контент и схему.
